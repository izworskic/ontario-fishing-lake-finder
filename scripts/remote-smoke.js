const {loadIndex,searchRemoteTrout,remoteMap}=require('../lib/remote');
(async()=>{
 const index=loadIndex();if(index.schemaVersion!==2)throw new Error(`Unexpected trout index schema ${index.schemaVersion}`);if(!Array.isArray(index.lakes)||index.lakes.length<500)throw new Error(`Trout index unexpectedly small: ${index.lakes?.length}`);if(!index.lakes.some(x=>x.evidence?.includes('ara_survey')))throw new Error('ARA survey-point evidence missing');if(!index.lakes.some(x=>x.evidence?.includes('stocking')))throw new Error('Stocking evidence missing');if(!index.lakes.every(x=>x.evidenceBySpecies))throw new Error('Species-specific provenance missing');
 const first=searchRemoteTrout({species:'Brook Trout',remote:'easy',sort:'remote'},{limit:25,offset:0}),second=searchRemoteTrout({species:'Brook Trout',remote:'easy',sort:'remote'},{limit:25,offset:25});if(first.candidateCount<100)throw new Error(`Brook Trout universe unexpectedly small: ${first.candidateCount}`);if(first.candidateCount!==second.candidateCount)throw new Error('Pagination changed candidateCount');if(!first.lakes.length)throw new Error('No Brook Trout rows returned');const lake=first.lakes[0];if(!Number.isFinite(lake.troutFit))throw new Error('Trout evidence score invalid');if(!lake.targetEvidence?.length)throw new Error('Target-species provenance missing');
 const map=remoteMap({species:'Brook Trout',remote:'easy'},'-96,41,-74,57',12000);if(!map.coverageComplete)throw new Error(`Province-wide Brook Trout map exceeded marker ceiling: ${map.matchedInView}`);if(map.matchedInView+map.unmappedMatching!==first.candidateCount)throw new Error(`Map/list universe mismatch: mapped ${map.matchedInView}, unmapped ${map.unmappedMatching}, list ${first.candidateCount}`);
 console.log(JSON.stringify({status:'ok',indexLakes:index.lakes.length,brookTrout:first.candidateCount,indexBuiltAt:index.builtAt,mappedBrookTrout:map.matchedInView,unmappedBrookTrout:map.unmappedMatching,sample:{name:lake.name,id:lake.id,troutFit:lake.troutFit,remoteContext:lake.remoteContext,confidence:lake.confidence,targetEvidence:lake.targetEvidence,roadKm:lake.remote?.roadKm,accessKm:lake.remote?.accessKm},summary:index.summary}));
})().catch(err=>{console.error(err);process.exit(1)});
