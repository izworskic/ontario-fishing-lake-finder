name: Catalog smoke

on:
  pull_request:
    paths:
      - 'api/**'
      - 'lib/**'
      - 'public/**'
      - 'scripts/catalog-smoke.js'
      - 'scripts/remote-smoke.js'
      - 'tests/**'
      - '.github/workflows/catalog-smoke.yml'
  push:
    branches: [main]
    paths:
      - 'api/**'
      - 'lib/**'
      - 'public/**'
      - 'scripts/catalog-smoke.js'
      - 'scripts/remote-smoke.js'
      - '.github/workflows/catalog-smoke.yml'
  workflow_dispatch:

jobs:
  catalog-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 6
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm test
      - run: node scripts/catalog-smoke.js
      - run: node scripts/remote-smoke.js
